# 修改记录

## 2026-09-25：1.0.1 安全与完整性修复

- 问题与目标：修复终端输入可能执行命令、复制遗漏分支、账号占位符、目录展开、并发导入串号、未完成就报完成、HTML 插值及表单校验不足；提供可直接重新加载的扩展目录与 ZIP。
- 原因与取舍：两处命令生成统一为共享模块；账号取自 gh 当前登录状态。后台串行处理任务，每项绑定标签页与页面身份，使用会话存储。没有可靠成功证据时只记录“已发起提交”，不虚报成功。
- 修改：shared.js、background.js、content_import.js、content_repo.js、popup.js、manifest.json、popup.html；新增回归测试、打包脚本、固定依赖与 CI、安全说明；更新 README、忽略清单和公开作者署名。版本升为 1.0.1，移除多余 tabs 权限。
- 验证：54 项自动检查通过；真实临时 Git 仓库的全部分支、轻量/附注标签与默认分支一致；Bash/Zsh 的危险输入、已有目录和各失败路径均受控。开发依赖审计 0 告警。Ego 独立空间的真实 DOM 交互验证：Public 状态不提交，Private 状态原生按钮产生且只产生一次提交事件。详见 [验证结果](docs/verification/1.0.1.json)。
- 失败与纠正：最初 UI 测试误按旧命令拼写断言，按实际账号调用修正；真实浏览器测试发现嵌套原生提交会被浏览器抑制，加入下一事件轮次后再校验和提交，并补回归。命令生成接口统一为“基础目录 + 目标名”，补测防止目录重复；源默认分支通过 gh 明确设置。
- 未验证：独立验证浏览器未登录 GitHub，未创建真实远端仓库；未执行真实 LFS 数据迁移；尚未重新加载用户 Chrome 中的扩展。ZIP 和本地目录用于用户更新。
- 遗留与限制：云端导入依赖 GitHub 表单结构，不能确认时会停止；Git 复制不含 Issues、PR、Releases 附件、设置或子模块私有化；失败可能留下空远端或部分本地副本，不自动删除用户资料。
- 回滚：修改前基线提交为 `70e7ead2d2e1f1b9d57ca80e332d23ed0a21ef8d`。可将该版本检出到另一个目录并在 Chrome 重新加载；不建议继续使用有已知缺陷的旧版本。不要在存在用户未提交修改时直接覆盖当前目录。
- 经验：输入转义、失败退出和结果确认是不同保护；私有性必须针对同一个真实表单检查，任务身份必须与并发消费和历史记录一起设计；自动测试与真实浏览器交互各自提供不同证据。

## 2026-09-26：提供可公开下载的 Chrome 扩展 ZIP

- 问题与目标：此前 ZIP 只在本地，GitHub 尚无 Releases 下载入口；用户希望别人下载后即可按说明安装。
- 原因与取舍：Chrome 手动安装须解压并加载目录，不能双击 ZIP 自动安装。沿用 1.0.1 运行代码，发布轻量扩展 ZIP，README 优先说明下载方式；不引入构建工具要求。
- 修改：README 顶部新增安装包下载链接和安装、更新步骤；新增包内 `安装说明.txt`、[发布说明](docs/releases/1.0.1.md)；打包白名单纳入安装说明。扩展 JS、权限与版本保持原 1.0.1。
- 验证：ZIP 完整性通过，16 个文件、30182 字节；包内文件逐字节匹配源码，manifest 与 popup 引用资源齐全，不含 Git 历史、测试及开发依赖；常见令牌、私钥、个人路径和邮箱模式扫描无命中。SHA-256：`61548cfbee4f2df87ceab1edb070d05960d46b8f88827bdfec20a777bcd2d901`。本轮只改文档及打包清单，运行代码复用上一轮 54 项测试证据；发布后的下载结果另行补记。
- 未验证与遗留：真实登录态的云端导入、真实 LFS 对象迁移仍未验证；手动安装仍需用户操作，不会自动更新。
- 回滚：本轮修改前提交 `8901582309fa7615f6a7e093b4e878056f5cfa72`；可逆向撤销本轮文档与打包改动。若需撤下下载，在 GitHub 撤下对应 Release 即可，不删除用户本地资料。
- 经验：本地生成 ZIP 不等于别人能下载；发布后需用未登录请求下载附件并比对字节，验证真正的公开入口。
- 发布核验：已将 `b6e828910ef6c13d2957148bfe9e9f5e74fec8cc` 发布为 [v1.0.1](https://github.com/ryumu1008/github-fork-to-private/releases/tag/v1.0.1)，ZIP 附件已公开。无登录、无认证的 HTTP 请求下载返回 200，下载内容与本地 ZIP 完全相同；GitHub 附件摘要也匹配上述 SHA-256。[本次 CI](https://github.com/ryumu1008/github-fork-to-private/actions/runs/36229577857) 通过。README 直接下载链接指向已核验附件。

## 2026-09-26：README 中英文双语

- 问题与目标：项目说明只有中文，补齐完整英文说明，方便两种语言的读者安装和使用。
- 原因与取舍：保留 `README.md` 为默认中文，新增 `README.en.md`，顶部互相链接。两份说明均明确扩展界面目前为中文；英文说明同时列出实际中文按钮及其含义，避免误以为扩展界面已翻译。
- 修改：英文版覆盖下载、安装、更新、两种复制方式、数据权限、复制限制、开发测试、许可与反馈；打包白名单新增英文 README，后续构建包含双语说明。已发布的 1.0.1 ZIP 保持发布时内容，本轮不替换附件或移动版本标签。
- 验证：中英文小节数量及命令一致，语言切换和本地文档链接有效，下载链接指向现有附件；在临时目录构建并校验 17 个文件，两个 README 都包含在内且内容匹配源码。临时目录已清理，既有本地发布 ZIP 的摘要未变；`git diff --check` 通过。
- 未验证与遗留：本轮未修改扩展运行代码或权限，未另做真实 GitHub 导入；之前记录的真实导入与 LFS 验证限制仍适用。
- 回滚：修改前提交 `0da63dd96d35a932b818e32bb0a3d096bfae97a7`；可撤销本轮 README、打包清单及记录变更，不影响已发布安装包。
