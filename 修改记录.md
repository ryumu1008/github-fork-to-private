# 修改记录

## 2026-09-25：1.0.1 安全与完整性修复

- 问题与目标：修复终端输入可能执行命令、复制遗漏分支、账号占位符、目录展开、并发导入串号、未完成就报完成、HTML 插值及表单校验不足；提供可直接重新加载的扩展目录与 ZIP。
- 原因与取舍：两处命令生成统一为共享模块；账号取自 gh 当前登录状态。后台串行处理任务，每项绑定标签页与页面身份，使用会话存储。没有可靠成功证据时只记录“已发起提交”，不虚报成功。
- 修改：shared.js、background.js、content_import.js、content_repo.js、popup.js、manifest.json、popup.html；新增回归测试、打包脚本、固定依赖与 CI、安全说明；更新 README、忽略清单和公开作者署名。版本升为 1.0.1，移除多余 tabs 权限。
- 验证：54 项自动检查通过；真实临时 Git 仓库的全部分支、轻量/附注标签与默认分支一致；Bash/Zsh 的危险输入、已有目录和各失败路径均受控。开发依赖审计 0 告警。Ego 独立空间的真实 DOM 交互验证：Public 状态不提交，Private 状态原生按钮产生且只产生一次提交事件。详见 [验证结果](docs/verification/1.0.1.json)。
- 失败与纠正：最初 UI 测试误按旧命令拼写断言，按实际账号调用修正；真实浏览器测试发现嵌套原生提交会被浏览器抑制，加入下一事件轮次后再校验和提交，并补回归。命令生成接口统一为“基础目录 + 目标名”，补测防止目录重复；源默认分支通过 gh 明确设置。
- 未验证：独立验证浏览器未登录 GitHub，未创建真实远端仓库；未执行真实 LFS 数据迁移；尚未重新加载用户 Chrome 中的扩展。ZIP 和本地目录用于用户更新。
- 遗留与限制：云端导入依赖 GitHub 表单结构，不能确认时会停止；Git 复制不含 Issues、PR、Releases 附件、设置或子模块私有化；失败可能留下空远端或部分本地副本，不自动删除用户资料。
- 回滚：修改前基线提交为 `70e7ead2d2e1f1b9d57ca80e332d23ed0a21ef8d`。可将该版本检出到另一个目录并在 Chrome 重新加载；不建议继续使用有已知缺陷的旧版本。不要在存在用户未提交修改时直接覆盖当前目录。
- 经验：输入转义、失败退出和结果确认是不同保护；私有性必须针对同一个真实表单检查，任务身份必须与并发消费和历史记录一起设计；自动测试与真实浏览器交互各自提供不同证据。

## 2026-09-26：提供可公开下载的 Chrome 扩展 ZIP

- 问题与目标：此前 ZIP 只在本地，GitHub 尚无 Releases 下载入口；用户希望别人下载后即可按说明安装。
- 原因与取舍：Chrome 手动安装须解压并加载目录，不能双击 ZIP 自动安装。沿用 1.0.1 运行代码，发布轻量扩展 ZIP，README 优先说明下载方式；不引入构建工具要求。
- 修改：README 顶部新增安装包下载链接和安装、更新步骤；新增包内 `安装说明.txt`、[发布说明](docs/releases/1.0.1.md)；打包白名单纳入安装说明。扩展 JS、权限与版本保持原 1.0.1。
- 验证：ZIP 完整性通过，16 个文件、30182 字节；包内文件逐字节匹配源码，manifest 与 popup 引用资源齐全，不含 Git 历史、测试及开发依赖；常见令牌、私钥、个人路径和邮箱模式扫描无命中。SHA-256：`61548cfbee4f2df87ceab1edb070d05960d46b8f88827bdfec20a777bcd2d901`。本轮只改文档及打包清单，运行代码复用上一轮 54 项测试证据；发布后的下载结果另行补记。
- 未验证与遗留：真实登录态的云端导入、真实 LFS 对象迁移仍未验证；手动安装仍需用户操作，不会自动更新。
- 回滚：本轮修改前提交 `8901582309fa7615f6a7e093b4e878056f5cfa72`；可逆向撤销本轮文档与打包改动。若需撤下下载，在 GitHub 撤下对应 Release 即可，不删除用户本地资料。
- 经验：本地生成 ZIP 不等于别人能下载；发布后需用未登录请求下载附件并比对字节，验证真正的公开入口。
- 发布核验：已将 `b6e828910ef6c13d2957148bfe9e9f5e74fec8cc` 发布为 [v1.0.1](https://github.com/ryumu1008/github-fork-to-private/releases/tag/v1.0.1)，ZIP 附件已公开。无登录、无认证的 HTTP 请求下载返回 200，下载内容与本地 ZIP 完全相同；GitHub 附件摘要也匹配上述 SHA-256。[本次 CI](https://github.com/ryumu1008/github-fork-to-private/actions/runs/36229577857) 通过。README 直接下载链接指向已核验附件。

## 2026-09-26：README 中英文双语

- 问题与目标：项目说明只有中文，补齐完整英文说明，方便两种语言的读者安装和使用。
- 原因与取舍：保留 `README.md` 为默认中文，新增 `README.en.md`，顶部互相链接。两份说明均明确扩展界面目前为中文；英文说明同时列出实际中文按钮及其含义，避免误以为扩展界面已翻译。
- 修改：英文版覆盖下载、安装、更新、两种复制方式、数据权限、复制限制、开发测试、许可与反馈；打包白名单新增英文 README，后续构建包含双语说明。已发布的 1.0.1 ZIP 保持发布时内容，本轮不替换附件或移动版本标签。
- 验证：中英文小节数量及命令一致，语言切换和本地文档链接有效，下载链接指向现有附件；在临时目录构建并校验 17 个文件，两个 README 都包含在内且内容匹配源码。临时目录已清理，既有本地发布 ZIP 的摘要未变；`git diff --check` 通过。
- 未验证与遗留：本轮未修改扩展运行代码或权限，未另做真实 GitHub 导入；之前记录的真实导入与 LFS 验证限制仍适用。
- 回滚：修改前提交 `0da63dd96d35a932b818e32bb0a3d096bfae97a7`；可撤销本轮 README、打包清单及记录变更，不影响已发布安装包。

## 2026-09-26：真实 GitHub 复制测试（网页流程待用户接续）

- 问题与目标：补上真实 GitHub 服务验证，检查复制内容和目标私有性，区别于前面的本地替身测试。
- 原因与取舍：Ego 测试浏览器在 GitHub 登录页，且尚未加载本扩展；已将同一测试空间交给用户完成登录与加载。独立测试无需网页登录的本地复制功能，直接执行扩展现有 `buildCliScript` 生成的原样脚本，不替换 GitHub 命令。
- 实际操作：以本公开项目为源，在已核实的 gh 登录账号下新建私有测试副本；在独立本地目录内复制，未修改扩展运行代码。测试输出和副本位于忽略目录 `dist/live-test-20260926/`，其中 `result.json` 保留测试仓库定位信息；可分享摘要见 [真实服务测试记录](docs/verification/live-2026-09-26.json)。
- 验证：脚本在 Zsh 中执行成功；GitHub API 确认目标为 private 且不是 fork；未认证请求返回 404；实际源库的 1 个分支、1 个标签及全部对应 Git 对象 ID 与目标一致，默认分支同为 main，upstream 推送地址为 DISABLED。测试源 main 为 `fe51eb5d9769e147fd7b687387907665902d8b57`。
- 未验证与遗留：网页按钮、扩展消息通道、真实云端导入尚未完成，需要用户在已交接浏览器中登录并加载扩展后继续；本机未安装 git-lfs，脚本已明确提示，此次不证明真实 LFS 迁移。
- 回滚与资源：测试没有改动源仓库；私有测试副本及本地副本保留供检查，没有删除用户资料。浏览器空间保留用于用户接续；本轮没有启动本地服务器。
- 经验：本地复制和网页导入是两条路径；其中一条真实成功，不能代表另一条也已通过。

## 2026-09-26：1.0.2 新版 GitHub 导入兼容及真实网页验收

- 问题与目标：用户完成网页登录和扩展加载后，真实点击仓库上的“复制到私有”按钮；1.0.1 能创建导入任务，但提示“未找到完整的 GitHub 导入表单”，未提交。当前 GitHub 已换成 React 导入表单，字段名称变化，HTML form 不再声明 action/method，由 GitHub 自己的事件处理器发送 POST。
- 原因与取舍：仅识别 GitHub `/new/import` 页面上 `repo-creation` 应用的已观察表单结构，同时保留旧 POST 表单支持。不调用私有接口、不读取或填写凭据。新版路径先阻止浏览器原生 GET，再让 GitHub 的事件处理器接收经过检查的提交；除单选框和 FormData 外，还检查 GitHub 渲染的私有状态提示。
- 修改：`content_import.js` 增加新版识别及提交保护，`tests/import.test.cjs` 补 11 项回归；manifest、弹窗、包版本、安装说明和中英文 README 升至 1.0.2；更新安全说明，新增双语发布说明和 [1.0.2 验证结果](docs/verification/1.0.2.json)。此前 CLI 验证记录保留，并补上网页验收链接。
- 验证：全部 65 项自动测试通过。加载实际扩展后，通过仓库按钮与后台任务发起云端导入，默认自动提交完成，GitHub 显示“Importing complete”。GitHub API 确认目标 private=true、fork=false，未认证访问返回 404；实际源库的 1 个分支、1 个标签及对应对象 ID 完全一致，默认分支为 main。测试源 main 为 `fe51eb5d9769e147fd7b687387907665902d8b57`，其后发布更新不属于本次复制内容。
- 打包：1.0.2 ZIP 共 17 个文件、34911 字节，包含双语 README；完整性、资源引用、逐字节源码匹配及常见敏感信息模式检查通过。SHA-256：`c68ace3bbbc099683156ebb9f02dd5113858f63956ce20bb7994cd0cdb72a0b5`。1.0.1 旧附件保留。
- 失败与纠正：最初测试因尚未加载扩展而交接用户；旧版表单识别失败后据真实 DOM 和 GitHub 公共前端代码适配。测试工具的跨 Shadow DOM 重新加载选择器无法命中，改为定位同一扩展卡片的实际按钮；直接打开弹窗页被浏览器拦截，回到真实仓库按钮测试。扩展生成的结果标签页未及时出现在测试空间列表，核实实际结果 URL 后在同一空间打开，最终通过真实页面及独立 API/Git 证据确认。
- 未验证与遗留：本机没有 git-lfs，真实 LFS 对象迁移仍未验证；本次浏览器为 Ego Chromium，用户另一个 Chrome 安装仍需手动重新加载。未来 GitHub 页面变更可能再次触发安全停止。
- 回滚与资源：修改前代码提交为 `fe51eb5d9769e147fd7b687387907665902d8b57`，可将旧版检出到另一目录加载，但旧版不兼容当前云端导入页。本次私有测试副本保留供用户核验，定位信息仅存忽略目录 `dist/live-test-20260926/`，公开报告不包含私有测试仓库地址。
- 经验：以真实登录页面为准；必须区分 HTML 表单的默认提交和站点自己的事件提交，兼容新版页面时不能撤掉私有检查或允许 GET 兜底。
- 发布与收尾核验：提交 `920de2d9a22ff299bfd8587135f0bf59fdead225` 已同步 GitHub 并发布为 [v1.0.2](https://github.com/ryumu1008/github-fork-to-private/releases/tag/v1.0.2)；未登录下载 ZIP 返回 HTTP 200，附件与本地文件逐字节一致，GitHub 摘要匹配；[发布代码的 CI](https://github.com/ryumu1008/github-fork-to-private/actions/runs/36231382934) 通过。测试浏览器已重新加载并读回版本 1.0.2，扩展卡片未显示错误；测试空间关闭回执为 closedSpace=true，未保留未管理标签页。本轮下载的临时 GitHub 前端脚本已清理，验证日志和私有测试副本保留用于复核。

## 2026-09-26：1.1.0 默认一键复制与自动填写确认

- 问题与目标：用户实际使用时看到源地址和名称为空、需要填表，希望只点一次就复制到 Private。截图本身不足以确定旧版本、刷新导入页还是 React 输入状态问题；另一个同名本地目录已不存在，用户常用 Chrome 的实际加载版本未能核实。
- 原因与取舍：1.0.2 仓库按钮先打开配置框，且文字只检查 DOM 显示值，未核对 GitHub 回写状态。新版改为直接开始，更多配置保留；先确认 Private，再逐项填写并核对受控值和名称检查。准备阶段最多重试三次，重试先清空再写入；倒计时阶段不重填，用户改动时停止。任务身份与提交保护保持。
- 修改：仓库按钮、插件弹窗、共享默认命名、后台同窗口打开和受控改名；默认名称冲突时依次编号，最多 20 个候选，手工名称不改动。同名重命名仅允许在已领取但未提交的同标签页、同页面任务上操作，历史同步最终名称。修复单页导航保留按钮时无法启动下一仓库的问题。版本、双语 README、安全说明、安装说明和发布说明更新为 1.1.0。
- 验证：76 项回归通过，包含受控字段未接收、延迟接收、Private 切换替换字段、同名编号、越权改名、重放、边界、连续点击和单页导航。独立只读审查通过任务绑定、并发改名与名称上限。真实 Ego 浏览器从同一公开仓库分别一次点击启动默认复制和重名复制，未操作导入表单，两次 GitHub 均显示 Importing complete；API 确認 private=true、fork=false，匿名 404，1 分支、2 标签及对象 ID 完全一致，默认 main。源快照为 bc9ed12c2002031f77633f87ff5df4f1bdba525a；后续发布更新不属于测试时源内容。
- 证据：公开摘要见 docs/verification/1.1.0.json；测试副本定位信息及 refs 保存在忽略目录 dist/oneclick-live.json，自动化输出 dist/oneclick-regression.tap。测试副本保留供检查。
- 失败与纠正：初次新增测试把名称提示挂在表单外，且旧 UI 断言命中了新增的外层提示；修正替身和选择器后通过。常用 Chrome 原生 UI 尝试因电脑控制工具禁止当前 URL 而结束，未绕过限制、未读取浏览器用户数据目录。Ego 独立空间中的实际插件与流程验证成功。
- 未验证与遗留：未确认用户截图故障的唯一根因；常用 Chrome 尚需重新加载并核对版本。私有源认证、真实 LFS 迁移未验收。导入结果仍由 GitHub 负责；重新加载已领取任务的导入页后需要回到仓库页发起。GitHub 页面结构或文案变化会安全停止。
- 回滚：本轮修改前提交 bc9ed12c2002031f77633f87ff5df4f1bdba525a，旧版安装包保留；需要回滚时检出到另一目录加载，不覆盖用户改动。1.1.0 不新增权限、依赖或开发者服务。
- 经验：显示已填不等于应用已接收；应把 GitHub 回写值、名称校验、私有性和实际复制结果分别验证。模拟 DOM 测试要读取应用提交数据，不能仅计数事件。
