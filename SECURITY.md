# 安全说明

建议使用 1.1.2 或更新版本。1.1.2 不再依赖固定英文提示判断私有性，而是核对 GitHub 回写的单选状态、实际选中值和表单数据；名称检查绑定当前完整名称与 GitHub 校验标记，更名前等待旧校验撤下，不明错误停止。带有当前名称的拒绝提示允许默认命名有限重试，但只有明确可用才提交；自定义名称不会被擅自更改。1.1.1 能恢复 Private 选中但 GitHub 仍为 Public 的状态，恢复期间阻止提交，校验不通过仍停止。1.1.0 额外核对 GitHub 回写的源地址、名称及名称可用性；默认同名自动加编号，改名仍绑定同一任务、标签页和页面，且只允许在提交前进行。1.0.1 无法识别新版 GitHub 导入表单时会停止辅助提交；1.0.2 已补充对应兼容与私有状态检查。1.0.0 的任务来源检查、命令输入处理和导入状态存在已知问题。

请勿在公开 Issue 中上传 Token、cookie、真实私有仓库内容或可直接滥用的漏洞细节。若仓库的 Security 页面提供私密漏洞报告入口，请使用该入口；否则可先提交不含敏感细节的问题概述，以便安排后续沟通。

本工具不会保证 GitHub 云端导入成功，也不会把一次提交直接记录为完成。GitHub 页面变化导致校验失败时会停止辅助提交。自动化测试不替代真实浏览器和 GitHub 服务的验证。
